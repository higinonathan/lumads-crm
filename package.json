{
  "name": "lumads-crm",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build && node -e \"const fs=require('fs');fs.mkdirSync('dist/.openai',{recursive:true});fs.copyFileSync('.openai/hosting.json','dist/.openai/hosting.json');fs.mkdirSync('dist/server',{recursive:true});fs.writeFileSync('dist/server/index.js','export default { async fetch(request, env) { return env.ASSETS.fetch(request); } };\\n')\""
  },
  "devDependencies": {
    "vite": "^8.2.2"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.112.4"
  }
}
